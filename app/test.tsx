import React, { useEffect, useState } from 'react';
import { Image, ScrollView, Text, View, Platform, Alert } from 'react-native';

export default function Test() {
    return(
        <View>
            <Text>Pas de scanner sur web</Text>
        </View>
    )
}